import React from 'react';

const TopNavbar = ({ title }) => (
    <div style={ {marginBottom: '1rem'} }>
         <p className="p__Inter">{title}</p>
         <div className="tp_cntent"></div>
        </div>
  );


export default TopNavbar